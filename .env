# 本地联调：关闭 mock，API 指向当前 Flask 后端（不修改任何前端代码）。
VUE_APP_API_MOCK=false
VUE_APP_API_BASE_URL=http://localhost:5000/api
# ALTCHA 挑战地址：自托管模式指向当前 Flask 后端；
# 使用 ALTCHA Sentinel 时改为 http://localhost:8888/v1/challenge?apiKey=<KEY>
VUE_APP_ALTCHA_CHALLENGE_URL=http://localhost:5000/api/auth/altcha-challenge
